[project]
name = "memoryvault"
version = "1.0.0"
description = "Local-first tool for learning memory strategies for long-running AI work"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mypy>=1.19.1",
]
